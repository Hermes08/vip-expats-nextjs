# Placeholder Image Required

Add a file named `placeholder-property.jpg` to this `/public` directory.

This image is used as a fallback for property listings that have no image URL.
Recommended: a neutral 800×600 navy-toned JPG (matches brand colors).

No `placeholder.com` URLs were found in the codebase at the time of the performance audit (2026-04-01),
so this is a preventive note only.
